{
  "name": "@assize/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --serve",
    "build:single": "node build.mjs --single"
  },
  "dependencies": {
    "@assize/protocol-types": "workspace:*",
    "@assize/reference": "workspace:*",
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
