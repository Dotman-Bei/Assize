node_modules/
dist/
*.tsbuildinfo
out/
cache/
cache_forge/
broadcast/
coverage/
.env
.env.local
.env.*.local
*.log
.DS_Store

# AGENTS.md hard block: never commit anything under internal/
internal/

# Generated G2 vectors. Never committed: a stale fixture would compare an old
# reference implementation against the current Solidity one and report a
# divergence that is not real. `pnpm test:differential` regenerates them on
# every run, which is why the gate is that command and not `forge test`.
contracts/test/fixtures/differential.*

# Pinned upstream, fetched by `pnpm skills:install`, verified against
# skills-lock.json. The lock is committed; the content is not ours to vendor.
.skills/
